import textTokenizingUtils from 'dummy/utils/text-tokenizing-utils';
import { module, test } from 'qunit';

module('Unit | Utility | text-tokenizing-utils', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = textTokenizingUtils();
    assert.ok(result);
  });
});
